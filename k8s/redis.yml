apiVersion: apps/v1
kind: Deployment
metadata:
  name: videor-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: videor-redis
  template:
    metadata:
      labels:
        app: videor-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: videor-redis
spec:
  selector:
    app: videor-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
